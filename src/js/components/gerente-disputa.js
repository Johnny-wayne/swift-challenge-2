document.addEventListener('DOMContentLoaded', () => {
  // O JavaScript pode ser usado no futuro para:
  // 1. Abrir um modal ao clicar em "Nova Disputa".
  // 2. Enviar os dados do formulário de nova disputa para o servidor.
  // 3. Implementar a funcionalidade do botão de fechar (x) nas disputas em andamento.

  console.log("Página de Disputas carregada.");
});